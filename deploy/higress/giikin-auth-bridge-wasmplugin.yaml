# giikin-auth-bridge — ai-agent API SSO Header 注入
# 前置：McpBridge 已注册 iam-session-redis；镜像已 push 至 ACR prod/giikin-auth-bridge:1.0.0
# 匹配 HiGress 控制台创建的路由 Ingress 名称 ai-agent-api（namespace test）
---
apiVersion: extensions.higress.io/v1alpha1
kind: WasmPlugin
metadata:
  name: giikin-auth-bridge-ai-agent
  namespace: test
  labels:
    higress.io/resource-definer: higress
spec:
  priority: 200
  phase: AUTHN
  failStrategy: FAIL_OPEN
  url: oci://giimall-acr-registry-vpc.cn-hangzhou.cr.aliyuncs.com/prod/giikin-auth-bridge:1.0.0
  defaultConfigDisable: true
  matchRules:
    - ingress:
        - ai-agent-api
      configDisable: false
      config:
        internal_key: "mvp-test-internal-key-2026"
        session_cookie_name: "guard_token"
        audit_enabled: false
        redis:
          service_name: "iam-session-redis.dns"
          service_port: 6379
          username: "test_giimall_redis"
          password: "REPLACE_WITH_IAM_REDIS_PASSWORD"
          database: 0
          timeout: 2000
