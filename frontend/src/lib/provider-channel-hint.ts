/**
 * 接入通道（provider）筛选的说明文案：与凭据/LiteLLM 路由一致；
 * 火山、DashScope 等聚合通道下可能含多种底模。
 */
export const PROVIDER_CHANNEL_FILTER_HINT_LONG =
  '与凭据/LiteLLM 路由一致；火山、DashScope 等通道下可能包含多种底模，可配合名称区分。'

export const PROVIDER_CHANNEL_FILTER_HINT_GATEWAY =
  '与凭据路由一致；火山、DashScope 等通道下可能含多种底模，可结合名称列区分。'
