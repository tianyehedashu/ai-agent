-- =============================================================================
-- 回滚脚本（可选）：不清除 created_by_user_id，避免数据丢失
-- =============================================================================

-- 回滚时不做任何操作，避免丢失已填充的创建者信息
SELECT 'No rollback operation performed to preserve data' AS result;
