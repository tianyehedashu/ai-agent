# 🎯 Agent 组织架构参考：Manus & CrewAI

> **版本**: 1.0.0
> **更新日期**: 2026-01-14
> **说明**: 参考 Manus 和 CrewAI 的架构设计，简化当前系统的 Agent 概念混乱问题

---

## 📋 目录

1. [问题分析](#问题分析)
2. [Manus 架构分析](#manus-架构分析)
3. [CrewAI 架构分析](#crewai-架构分析)
4. [对比分析](#对比分析)
5. [简化方案](#简化方案)
6. [实现建议](#实现建议)

---

## 问题分析

### 1.1 当前困惑

```
❓ 当前系统中有太多 Agent 概念：
   • Agent Definition（定义）
   • Agent Instance（实例）
   • Agent Deployment（应用）
   • Agent Engine（引擎）
   • Agent-to-Agent（A2A）
   • 工作流中的 Agent 节点

❓ 如何组织多个 Agent？
   • 多个 Agent 如何协作？
   • 如何分配任务？
   • 如何管理 Agent 团队？
```

### 1.2 核心问题

**当前系统缺少 Agent 的组织概念**，导致：
- ❌ Agent 概念混乱（定义、实例、应用混在一起）
- ❌ 不知道如何组织多个 Agent 协作
- ❌ 缺少类似 CrewAI 的 Crew（团队）概念
- ❌ 缺少类似 Manus 的角色分工概念

---

## Manus 架构分析

### 2.1 Manus 多代理架构

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        Manus 多代理架构                                       │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  用户请求                                                                   │
│     │                                                                       │
│     ▼                                                                       │
│  ┌─────────────────────┐                                                   │
│  │  Planner Agent      │ ◀── 规划代理：分析请求，创建执行计划                │
│  │  (规划代理)          │                                                   │
│  └──────────┬──────────┘                                                   │
│             │                                                               │
│             │ 执行计划                                                       │
│             ▼                                                               │
│  ┌─────────────────────┐                                                   │
│  │ Execution Agent     │ ◀── 执行代理：执行具体任务                         │
│  │ (执行代理)           │     • 浏览器操作                                 │
│  └──────────┬──────────┘     • 数据库操作                                 │
│             │                 • 代码执行                                   │
│             │                                                               │
│             │ 执行结果                                                       │
│             ▼                                                               │
│  ┌─────────────────────┐                                                   │
│  │ Knowledge Agent     │ ◀── 知识代理：处理信息检索                        │
│  │ (知识代理)           │     • 上下文理解                                 │
│  └──────────┬──────────┘     • 信息维护                                   │
│             │                                                               │
│             │ 验证请求                                                       │
│             ▼                                                               │
│  ┌─────────────────────┐                                                   │
│  │ Verification Agent  │ ◀── 验证代理：审查工作质量                        │
│  │ (验证代理)           │     • 质量检查                                   │
│  └──────────┬──────────┘     • 错误检测                                   │
│             │                                                               │
│             │ 最终结果                                                       │
│             ▼                                                               │
│  交付成果                                                                   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 2.2 Manus 核心特点

**1. 角色分工明确**
- 每个 Agent 有明确的职责（规划、执行、知识、验证）
- 职责不重叠，各司其职

**2. 流水线协作**
- 按顺序执行：规划 → 执行 → 知识 → 验证
- 每个阶段的结果传递给下一个阶段

**3. 并行处理**
- 执行阶段可以并行处理多个子任务
- 提高效率

**4. 质量保证**
- 验证代理确保输出质量
- 可以回退到前面的阶段

### 2.3 Manus 架构优势

```
✅ 职责清晰：每个 Agent 只做一件事
✅ 易于理解：流水线式协作，逻辑清晰
✅ 易于扩展：添加新角色不影响现有架构
✅ 质量保证：验证环节确保输出质量
```

---

## CrewAI 架构分析

### 3.1 CrewAI 核心概念

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        CrewAI 核心概念                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  🤖 Agent (代理)                                                            │
│  ─────────────────────────────────────────────────────────────────────────  │
│  具有特定角色和能力的智能体                                                  │
│                                                                             │
│  属性:                                                                      │
│  ├─ role: 角色（如：研究员、分析师、写作者）                                │
│  ├─ goal: 目标（如：发现创新技术）                                          │
│  ├─ backstory: 背景故事（定义 Agent 的性格和能力）                          │
│  ├─ tools: 工具列表                                                        │
│  ├─ llm: 使用的模型                                                        │
│  └─ memory: 是否启用记忆                                                   │
│                                                                             │
│  📋 Task (任务)                                                             │
│  ─────────────────────────────────────────────────────────────────────────  │
│  Agent 需要完成的具体工作项                                                 │
│                                                                             │
│  属性:                                                                      │
│  ├─ description: 任务描述                                                  │
│  ├─ expected_output: 期望输出格式                                          │
│  ├─ agent: 负责的 Agent                                                    │
│  ├─ context: 依赖的前置任务（任务间依赖）                                  │
│  └─ async_execution: 是否异步执行                                         │
│                                                                             │
│  👥 Crew (团队)                                                             │
│  ─────────────────────────────────────────────────────────────────────────  │
│  一组 Agent 和 Task 的组合，定义执行策略                                    │
│                                                                             │
│  属性:                                                                      │
│  ├─ agents: Agent 列表                                                      │
│  ├─ tasks: Task 列表                                                        │
│  ├─ process: 执行流程 (sequential/hierarchical/consensual)                │
│  └─ verbose: 是否输出详细日志                                              │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 3.2 CrewAI 执行流程

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        CrewAI 执行流程                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  1. 定义 Agent                                                              │
│  ────────────────────────────────────────────────────────────────────────  │
│  researcher = Agent(                                                        │
│      role="Senior Research Analyst",                                        │
│      goal="Discover innovative AI technologies",                            │
│      backstory="You are a veteran researcher...",                          │
│      tools=[search_tool],                                                   │
│      llm="gpt-4o"                                                          │
│  )                                                                          │
│                                                                             │
│  2. 定义 Task                                                               │
│  ────────────────────────────────────────────────────────────────────────  │
│  research_task = Task(                                                      │
│      description="Research latest AI developments",                         │
│      expected_output="A comprehensive research report",                     │
│      agent=researcher                                                       │
│  )                                                                          │
│                                                                             │
│  3. 组建 Crew                                                               │
│  ────────────────────────────────────────────────────────────────────────  │
│  crew = Crew(                                                               │
│      agents=[researcher, writer],                                           │
│      tasks=[research_task, writing_task],                                  │
│      process=Process.sequential  # 顺序执行                                 │
│  )                                                                          │
│                                                                             │
│  4. 执行                                                                    │
│  ────────────────────────────────────────────────────────────────────────  │
│  result = crew.kickoff()                                                    │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 3.3 CrewAI 执行模式

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        CrewAI 执行模式                                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  Sequential (顺序执行)                                                      │
│  ────────────────────────────────────────────────────────────────────────  │
│  Task 1 → Task 2 → Task 3                                                  │
│  每个任务按顺序执行，结果传递给下一个任务                                    │
│                                                                             │
│  Hierarchical (层级执行)                                                    │
│  ────────────────────────────────────────────────────────────────────────  │
│  Manager Agent                                                              │
│    ├─ Task 1 (Agent A)                                                     │
│    ├─ Task 2 (Agent B)                                                     │
│    └─ Task 3 (Agent C)                                                     │
│  Manager Agent 分配任务给各个 Agent                                         │
│                                                                             │
│  Consensual (共识执行)                                                      │
│  ────────────────────────────────────────────────────────────────────────  │
│  Agent A ↔ Agent B ↔ Agent C                                               │
│  Agent 之间协商决策，达成共识后执行                                          │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 3.4 CrewAI 架构优势

```
✅ 概念清晰：Agent、Task、Crew 三个核心概念
✅ 灵活组织：通过 Crew 灵活组织多个 Agent
✅ 任务驱动：以 Task 为中心，明确任务分配
✅ 支持多种执行模式：顺序、层级、共识
✅ 易于理解：代码即文档，逻辑清晰
```

---

## 对比分析

### 4.1 架构对比

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           架构对比表                                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  维度              │ Manus              │ CrewAI            │ 当前系统      │
│  ────────────────────────────────────────────────────────────────────────  │
│  Agent 组织        │ 角色分工            │ Crew（团队）       │ ❌ 缺少       │
│  Agent 定义        │ 角色明确            │ role + goal       │ ✅ 有定义     │
│  任务分配          │ 规划代理分配        │ Task 驱动          │ ❌ 不明确     │
│  执行模式          │ 流水线              │ 多种模式           │ ⚠️ 单一      │
│  协作机制          │ 顺序传递            │ 任务依赖           │ ⚠️ A2A       │
│  质量保证          │ 验证代理            │ 任务验证           │ ⚠️ 不明确     │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 4.2 核心差异

**Manus 方式**：
- 强调**角色分工**（规划、执行、知识、验证）
- 流水线式协作
- 适合**自动化任务处理**

**CrewAI 方式**：
- 强调**团队协作**（Crew 组织多个 Agent）
- 任务驱动（Task 分配）
- 适合**复杂项目协作**

**当前系统**：
- ❌ 缺少 Agent 组织概念
- ❌ 不知道如何组织多个 Agent
- ⚠️ 只有 A2A 调用，没有团队概念

---

## 简化方案

### 5.1 核心建议

**借鉴 CrewAI 的 Crew 概念，简化 Agent 组织**

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                       简化后的架构                                            │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  Layer 1: Agent Template (Agent 模板)                                     │
│  ────────────────────────────────────────────────────────────────────────  │
│  • 定义 Agent 的能力（role, goal, tools, llm）                             │
│  • 存储在 agents 表                                                        │
│  • 类似 CrewAI 的 Agent 定义                                               │
│                                                                             │
│  Layer 2: Crew (团队)                                                      │
│  ────────────────────────────────────────────────────────────────────────  │
│  • 组织多个 Agent 协作                                                     │
│  • 定义执行流程（sequential/hierarchical）                                 │
│  • 存储在 crews 表                                                         │
│  • 类似 CrewAI 的 Crew                                                    │
│                                                                             │
│  Layer 3: Task (任务)                                                      │
│  ────────────────────────────────────────────────────────────────────────  │
│  • 具体的工作项                                                            │
│  • 分配给特定的 Agent                                                      │
│  • 存储在 tasks 表                                                        │
│  • 类似 CrewAI 的 Task                                                    │
│                                                                             │
│  Layer 4: Execution (执行)                                                  │
│  ────────────────────────────────────────────────────────────────────────  │
│  • 基于 Crew 执行任务                                                      │
│  • 管理执行状态                                                            │
│  • 存储在 executions 表                                                   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 5.2 数据模型设计

```python
# ==================== Agent Template ====================

class Agent(BaseModel):
    """Agent 模板 - 定义 Agent 的能力"""

    __tablename__ = "agents"

    id: UUID
    user_id: UUID
    name: str

    # 核心属性（借鉴 CrewAI）
    role: str                   # 角色（如：研究员、分析师）
    goal: str                   # 目标（如：发现创新技术）
    backstory: str              # 背景故事（定义性格和能力）

    # 能力配置
    tools: list[str]            # 工具列表
    llm: str                    # 使用的模型
    memory: bool = False         # 是否启用记忆

    # 元数据
    created_at: datetime
    updated_at: datetime


# ==================== Crew (团队) ====================

class Crew(BaseModel):
    """Crew - 组织多个 Agent 协作"""

    __tablename__ = "crews"

    id: UUID
    user_id: UUID
    name: str
    description: str

    # Agent 列表
    agent_ids: list[UUID]       # 关联的 Agent ID 列表

    # 执行流程
    process: str                # sequential, hierarchical, consensual

    # 配置
    verbose: bool = False       # 是否输出详细日志

    # 元数据
    created_at: datetime
    updated_at: datetime

    # 关系
    agents: list[Agent] = relationship(...)
    tasks: list["Task"] = relationship(...)


# ==================== Task (任务) ====================

class Task(BaseModel):
    """Task - 具体的工作项"""

    __tablename__ = "tasks"

    id: UUID
    crew_id: UUID               # 关联的 Crew
    agent_id: UUID              # 负责的 Agent

    # 任务描述
    description: str            # 任务描述
    expected_output: str        # 期望输出格式

    # 任务依赖
    context_task_ids: list[UUID] = []  # 依赖的前置任务

    # 执行配置
    async_execution: bool = False  # 是否异步执行

    # 状态
    status: str                 # pending, running, completed, failed

    # 元数据
    created_at: datetime
    completed_at: datetime | None = None

    # 关系
    crew: Crew = relationship(...)
    agent: Agent = relationship(...)
    context_tasks: list["Task"] = relationship(...)


# ==================== Execution (执行) ====================

class Execution(BaseModel):
    """Execution - 执行记录"""

    __tablename__ = "executions"

    id: UUID
    crew_id: UUID               # 关联的 Crew
    user_id: UUID

    # 执行状态
    status: str                 # pending, running, completed, failed

    # 执行结果
    result: dict | None = None   # 执行结果

    # 时间戳
    created_at: datetime
    started_at: datetime | None = None
    completed_at: datetime | None = None

    # 关系
    crew: Crew = relationship(...)
    task_executions: list["TaskExecution"] = relationship(...)
```

### 5.3 关系图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           数据关系图                                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  User                                                                       │
│    │                                                                        │
│    │ 1:N                                                                    │
│    ▼                                                                        │
│  Agent (Template) ──────┐                                                 │
│    │                     │                                                 │
│    │ N:M                 │                                                 │
│    ▼                     │                                                 │
│  Crew ───────────────────┘                                                 │
│    │                                                                        │
│    │ 1:N                                                                    │
│    ▼                                                                        │
│  Task                                                                       │
│    │                                                                        │
│    │ N:1                                                                    │
│    ▼                                                                        │
│  Agent (负责执行)                                                            │
│                                                                             │
│  Crew                                                                       │
│    │                                                                        │
│    │ 1:N                                                                    │
│    ▼                                                                        │
│  Execution                                                                  │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 实现建议

### 6.1 代码示例

```python
# ==================== 定义 Agent ====================

# 研究员 Agent
researcher = Agent(
    id=uuid.uuid4(),
    name="研究员",
    role="Senior Research Analyst",
    goal="Discover innovative AI technologies and market trends",
    backstory="You are a veteran technology researcher...",
    tools=["search", "web_scraper"],
    llm="claude-3-5-sonnet-20241022",
    memory=True,
)

# 分析师 Agent
analyst = Agent(
    id=uuid.uuid4(),
    name="分析师",
    role="Data Analyst",
    goal="Analyze research data and extract insights",
    backstory="You are an expert data analyst...",
    tools=["data_analysis", "visualization"],
    llm="claude-3-5-sonnet-20241022",
    memory=True,
)

# 写作者 Agent
writer = Agent(
    id=uuid.uuid4(),
    name="写作者",
    role="Tech Content Strategist",
    goal="Transform findings into compelling narratives",
    backstory="You are a seasoned tech writer...",
    tools=["text_editor"],
    llm="claude-3-5-sonnet-20241022",
    memory=True,
)

# ==================== 定义 Task ====================

research_task = Task(
    id=uuid.uuid4(),
    crew_id=crew.id,
    agent_id=researcher.id,
    description="Research the latest AI agent developments",
    expected_output="A comprehensive research report",
    context_task_ids=[],
)

analysis_task = Task(
    id=uuid.uuid4(),
    crew_id=crew.id,
    agent_id=analyst.id,
    description="Analyze the research findings",
    expected_output="Data analysis with insights",
    context_task_ids=[research_task.id],  # 依赖研究任务
)

writing_task = Task(
    id=uuid.uuid4(),
    crew_id=crew.id,
    agent_id=writer.id,
    description="Write an executive summary",
    expected_output="A 500-word executive summary",
    context_task_ids=[research_task.id, analysis_task.id],  # 依赖前两个任务
)

# ==================== 组建 Crew ====================

crew = Crew(
    id=uuid.uuid4(),
    name="AI 研究团队",
    description="研究 AI 技术趋势并生成报告",
    agent_ids=[researcher.id, analyst.id, writer.id],
    process="sequential",  # 顺序执行
    verbose=True,
)

# ==================== 执行 ====================

execution = await crew_service.execute(
    crew_id=crew.id,
    user_id=user.id,
)

# 执行流程：
# 1. 执行 research_task (researcher)
# 2. 执行 analysis_task (analyst, 使用 research_task 的结果)
# 3. 执行 writing_task (writer, 使用前两个任务的结果)
```

### 6.2 服务层实现

```python
# backend/services/crew_service.py

class CrewService:
    """Crew 服务"""

    async def execute(
        self,
        crew_id: UUID,
        user_id: UUID,
    ) -> Execution:
        """执行 Crew"""
        # 1. 获取 Crew
        crew = await self.get(crew_id)

        # 2. 创建执行记录
        execution = Execution(
            id=uuid.uuid4(),
            crew_id=crew.id,
            user_id=user_id,
            status="running",
        )
        await self.db.save(execution)

        # 3. 根据 process 执行
        if crew.process == "sequential":
            result = await self._execute_sequential(crew, execution)
        elif crew.process == "hierarchical":
            result = await self._execute_hierarchical(crew, execution)
        elif crew.process == "consensual":
            result = await self._execute_consensual(crew, execution)

        # 4. 更新执行状态
        execution.status = "completed"
        execution.result = result
        execution.completed_at = datetime.utcnow()
        await self.db.save(execution)

        return execution

    async def _execute_sequential(
        self,
        crew: Crew,
        execution: Execution,
    ) -> dict:
        """顺序执行"""
        results = {}

        # 获取所有任务，按依赖关系排序
        tasks = await self._get_ordered_tasks(crew.id)

        for task in tasks:
            # 执行任务
            task_result = await self._execute_task(task, execution, results)
            results[task.id] = task_result

        return results

    async def _execute_task(
        self,
        task: Task,
        execution: Execution,
        context_results: dict,
    ) -> dict:
        """执行单个任务"""
        # 1. 获取 Agent
        agent = await self.agent_service.get(task.agent_id)

        # 2. 构建上下文（从前置任务获取结果）
        context = {}
        for context_task_id in task.context_task_ids:
            if context_task_id in context_results:
                context[context_task_id] = context_results[context_task_id]

        # 3. 创建 Agent Engine
        engine = AgentEngine(
            agent=agent,
            llm_gateway=self.llm_gateway,
            tool_registry=self.tool_registry,
        )

        # 4. 执行
        result = await engine.run(
            user_input=task.description,
            context=context,
        )

        return result
```

### 6.3 API 设计

```python
# backend/api/v1/crew.py

router = APIRouter(prefix="/crews", tags=["Crews"])

@router.post("/")
async def create_crew(
    data: CrewCreateRequest,
    current_user: User = Depends(get_current_user),
):
    """创建 Crew"""
    return await crew_service.create(
        user_id=current_user.id,
        **data.model_dump()
    )

@router.post("/{crew_id}/execute")
async def execute_crew(
    crew_id: UUID,
    current_user: User = Depends(get_current_user),
):
    """执行 Crew"""
    execution = await crew_service.execute(
        crew_id=crew_id,
        user_id=current_user.id,
    )
    return execution
```

---

## 总结

### 核心建议

1. **借鉴 CrewAI 的 Crew 概念**
   - 用 Crew 组织多个 Agent
   - 用 Task 分配具体工作
   - 简化 Agent 概念（只保留 Template）

2. **简化 Agent 概念**
   - Agent = Template（模板）
   - 不再区分 Definition、Instance、Deployment
   - 通过 Crew 和 Task 组织使用

3. **清晰的执行流程**
   - Crew → Task → Agent → Execution
   - 支持多种执行模式（顺序、层级、共识）

### 架构对比

```
当前架构（复杂）:               简化后架构（清晰）:
─────────────────              ─────────────────
Agent Definition              Agent (Template)
Agent Instance                Crew (团队)
Agent Deployment              Task (任务)
Agent Engine                  Execution (执行)
A2A 调用
```

### 实施步骤

1. **短期**: 添加 Crew 和 Task 模型
2. **中期**: 实现 Crew 执行服务
3. **长期**: 完善多种执行模式

---

<div align="center">

**借鉴 CrewAI，简化 Agent 组织：Crew + Task + Agent**

*文档版本: v1.0.0 | 最后更新: 2026-01-14*

</div>
