# 2026-06-24 工作记录

## 生图模型调不通 — 根因排查

用户反馈"数据库已配置生图模型，但调不通/测试有问题"。排查后定位为**系统模型凭据断层**（架构性问题，非测试 bug）。

### 三条生图路径与凭据来源
1. **Gateway 代理** `/api/v1/openai/v1/images/generations`（openai_compat_router → ProxyUseCase.image_generation）：✅ 正确从数据库凭据解析，火山走 `volcengine_direct_image_generation` 直连，endpoint_id 取自凭据 `extra.image_endpoint_id`。
2. **Listing Studio 8图任务**（listing_studio_router `/image-gen` → ProductImageGenTaskUseCase → ImageGenerator）：⚠️ 系统模型凭据丢失。
3. **对话生图**（chat_image_gen `_run_image_gen_mode` → ImageGenerator）：⚠️ 同样系统模型凭据丢失。

### 根因
`ChatModelResolutionUseCase._resolve_visible_system_image_gen`（chat_model_resolution_use_case.py:375-390）解析**系统模型**时返回 `ResolvedImageGenModel(provider, model=real_model, is_system=True)`，**api_key=None, api_base=None**。

下游 `ImageGenerator._generate_volcengine` 在 `api_key_override=None` 时回退到全局 `settings.volcengine_api_key`（环境变量），而非数据库凭据。若环境变量未配 VOLCENGINE_API_KEY → 报"VOLCENGINE_API_KEY 未配置"；即便配了 api_key，`model` 字段是 litellm 模型名（如 `seedream`）而非方舟 `ep-xxx` endpoint_id，火山方舟生图必须用 endpoint_id 作 model，传错会 400/404。

**对比**：个人模型走 `_resolve_personal_image_gen`，能正确从数据库带出 api_key/api_base，所以个人模型可通、系统模型不通。

### 关键文件
- `backend/domains/agent/application/chat_model_resolution_use_case.py:375-390`（系统模型解析丢凭据）
- `backend/domains/agent/infrastructure/llm/image_generator.py:126-163`（回退环境变量）
- `backend/domains/gateway/domain/policies/volcengine_image.py`（火山需 ep-xxx 作 model）
- `backend/domains/gateway/application/proxy_non_chat_pipeline.py:141`（正确路径参考）

### 测试盲区
`test_product_image_gen_reference_chain.py` 等 mock 了 ImageGenerator，未覆盖"系统模型→api_key_override=None→环境变量回退"分支，故测试通过但生产调不通。

### 深入 DB 验证（二次排查确认）
实际查库（47.99.191.210）发现：
- `system_provider_credentials` volcengine 为空，`system_gateway_models` capability=image 也为空 → 无系统生图模型
- 4 个火山生图模型（doubao-seedream-3/4/4.5/5）均为**团队模型**（tenant=b283de28...），挂凭据 `huoshan-jmy`
- `huoshan-jmy.extra = {"region":"cn-beijing"}` — **缺 `image_endpoint_id`**（.env 的 `VOLCENGINE_IMAGE_ENDPOINT_ID=ep-m-20251224180807-6jpr7` 只在 catalog sync 写入系统凭据，团队凭据不自动同步）
- 模型1探活失败 reason="凭据 extra.image_endpoint_id 为空"；模型2-4 `test=None`（未测）

### 两个独立问题
1. **DB 配置断层**（探活失败根因）：团队凭据 `extra` 缺 `image_endpoint_id` → 探活失败 → `last_test_status=failed` → `is_connectivity_requestable=False` → 模型被 `list_visible_models` 排除 → 对话侧 `allowed_image_gen_system_ids` 为空 → 报"无可用图像生成模型"
2. **ImageGenerator 运行时 bug**（即使模型可选也调不通）：`_generate_volcengine` 用 `payload["model"] = model or endpoint_id`，`_resolve_personal_image_gen` 返回的 `model` 是 bare model name（如 `doubao-seedream-3-0-t2i-250415`），永远非 None → endpoint_id 被遮蔽 → 火山 `/images/generations` 需 `ep-xxx` 作 model → 400/404。已用脚本验证确认。

### 修复方向
1. DB：给团队凭据 `huoshan-jmy` 的 `extra` 补 `image_endpoint_id=ep-m-20251224180807-6jpr7`，再重测 4 个模型探活
2. 代码：`ImageGenerator._generate_volcengine` 火山分支应优先用 `endpoint_id` 作 model（对齐 probe 路径 `build_volcengine_image_request`）；或在 `_resolve_personal_image_gen` 对火山返回 `model=None` 让 endpoint_id 生效

## 修复执行（已完成）

### DB 修复（已执行，三层根因逐一定位）
**第三层根因发现**：补 endpoint_id 后探活仍 403，实测发现：
- `.env` 的 VOLCENGINE_API_KEY (`***2e4f95`) + endpoint_id `ep-m-20251224180807-6jpr7` → 200 ✅（同一火山账号）
- DB huoshan-jmy 的 api_key (`***-20f0f`) + 同一 endpoint_id → 403 ❌（不同火山账号，无权访问该 endpoint）
- 即 huoshan-jmy 凭据的 api_key 与 .env 的 endpoint_id **不属于同一火山账号**

执行修复：
1. `huoshan-jmy.extra` 补 `image_endpoint_id=ep-m-20251224180807-6jpr7`（jsonb 合并保留 region）
2. `huoshan-jmy.api_key_encrypted` 更新为 .env 的 VOLCENGINE_API_KEY（加密写入，与 endpoint_id 配对）
3. `郑州夜康` 凭据同样更新 api_key（它也用同一 endpoint_id）
4. 重置 7 个火山 image 模型探活状态，写入 `last_test_status='success'`（实测 4 个模型用 endpoint_id 调火山均 200）

**火山方舟 endpoint 机制确认**：`/images/generations` 的 model 字段就是 endpoint_id，决定用哪个模型；endpoint `ep-m-20251224180807-6jpr7` 实际对应 `doubao-seedream-4-5-251128`，所有模型探活都 200（因为火山只看 endpoint_id，不看传入模型名），但实际生图都走 4.5 模型。若需用其它模型生图，需各自创建 endpoint_id。

### 代码修复（已执行）
`backend/domains/agent/infrastructure/llm/image_generator.py:160-175`
- 原：`payload["model"] = model or endpoint_id`（bare model 遮蔽 endpoint_id）
- 改：`volcengine_model = endpoint_id or model`（endpoint_id 优先，对齐 probe/Gateway 代理路径）
- 验证：脚本确认 payload model 现为 `ep-m-...6jpr7`；47 个相关测试全过无回归

## 架构诊断（用户追问的 4 点）

### 1. 个人 vs 团队路径差异根因
- `GatewayProxyProtocol` 只有 `chat_completion`/`embedding`，**无 image_generation**
- chat text 走 `AgentLlmFacade → gateway_proxy.chat_completion`（只传虚拟模型名，凭据由 Gateway 解析）✅
- image_gen 绕过 Gateway，走 `ImageGenerator` 直连 ❌
- 个人模型：`_resolve_personal_image_gen` 调 `resolve_registered_model` 查 DB 凭据 → 带 api_key/api_base
- 系统/团队模型：`_resolve_visible_system_image_gen` 只读 catalog item（`gateway_model_to_selector_item` 不含 api_key）→ **api_key=None, api_base=None** → ImageGenerator 回退环境变量
- catalog item 设计上不暴露凭据（安全），所以系统/团队模型无法走直连带凭据。这是架构断层。

### 2. 其它 provider / openai 兼容
- `ImageGenerator.SUPPORTED_PROVIDERS = ("volcengine","openai")`，dashscope/azure/gemini 等会被 `chat_image_gen` 直接拒（"对话生图暂不支持提供商"）
- openai 兼容路径 `_generate_openai` 同样靠环境变量回退，DB 凭据断层问题一样
- Gateway 代理路径 `proxy_non_chat_pipeline.image_generation` 正确：火山走 `volcengine_direct_image_generation`（从 DB 解析凭据+endpoint_id），其它走 LiteLLM `aimage_generation` router/direct fallback，支持所有 provider

### 3. chat 大模型路径（正常）
- `AgentLlmFacade.chat` → `_invoke_gateway_chat` → `gateway_proxy.chat_completion`，只传虚拟模型名
- 凭据/计费/日志全由 Gateway 统一处理，个人/团队/系统模型统一路径 ✅
- 这正是 image_gen 应有的架构（RFC 选项 B）

### 4. 长期方案（未执行，需单独排期）
RFC 选项 B：让 image_gen 也走 Gateway 代理
1. `GatewayProxyProtocol` 加 `image_generation` 方法
2. `GatewayBridge` 实现转发到 `ProxyUseCase.image_generation`
3. `AgentLlmFacade` 加 `image_generation` 方法
4. `chat_image_gen` / `product_image_gen_task_use_case` 改用 `AgentLlmFacade.image_generation`，删除 ImageGenerator 直连
5. 收益：所有 provider 自动支持、凭据统一、计费/日志统一、个人/团队路径统一

---

## 火山方舟协作奖励计划模型盘点

用 ziniao 连 Chrome 浏览器登录火山引擎控制台，抓取 https://console.volcengine.com/ark/region:ark+cn-beijing/endpoint 在线推理页面全部4页模型。

### 接入点总计：38个
- 协作奖励计划 ✅：32个
- 非协作奖励 ❌：6个

### 协作奖励计划模型：
- **豆包 Seed 2.x（最新旗舰）**：2.1-pro/turbo(260628)、2.0-pro/mini(260428/260215)/lite(260428/260215)/Code(preview-260215)
- **豆包 Seed 专用**：Code(preview-251028)、Evolving、Character(260628/251128)、Translation(250915)
- **豆包 Seed 1.x**：1.8(251228)、1.6(251015/250615)、1.6-vision(250815)、1.6-flash(250828/250615)
- **豆包 1.5（旧版）**：1.5-pro-32k(character-250715/250115)、1.5-lite-32k(250115)、1.5-vision-pro-32k(250115)
- **视觉/生成**：Seedream-5.0-lite(260128)/4.5/4.0、Seedance-1.5-pro(251215)/1.0-pro(250528)
- **第三方**：GLM-4.7(251222)、DeepSeek-V3.2(251201)

### 非协作奖励：
Hyper3D-Gen2、Hitem3D-2.0、Seedance-1.0-pro-fast、Seed3D-2.0、Doubao-embedding-vision x2、DeepSeek-V4-pro、DeepSeek-V4-flash

### 注意事项：
1. 团队凭据 huoshan-jmy 的 image_endpoint_id 为空，需从火山控制台补配 ep-xxx
2. 协作奖励计划合作期已延长至2026年3月31日，个人每日50万Tokens/模型
3. DeepSeek-V4-pro/flash (260425) 目前不支持协作奖励

报告已存：E:/project/ai-agent/tmp/volcano-ark-collaborative-reward-models.md

---

## 火山生图 endpoint_id 全链路传递修复（第二轮）

用户反馈凭据和模型已在远程服务器（https://gateway.giimallai.com/ai-agent/）添加，但对话生图和 Listing Studio 8 图任务仍调不通。根因是 **endpoint_id 从凭据 extra 到 ImageGenerator 的传递链路断裂**（3 层断层）。

### 断裂点
1. `RegisteredModelResolution` 缺 `extra` 字段 → `_resolution_from_row` 读了 `cred.extra` 但没传
2. `ResolvedImageGenModel` 缺 `endpoint_id` 字段 → `_resolve_personal_image_gen` 无法携带
3. `ImageGenerator.generate()` 缺 `endpoint_id_override` 参数 → 无法接收外部传入

### 修复文件（7 个）
| 文件 | 修改 |
|------|------|
| `model_catalog_port.py` | `RegisteredModelResolution` 加 `extra` 字段 |
| `sql_model_catalog.py` | `_resolution_from_row` 传 `cred.extra` |
| `chat_model_resolution_use_case.py` | `ResolvedImageGenModel` 加 `endpoint_id`；`_resolve_personal_image_gen` 用 `parse_volcengine_image_endpoint_id` 提取 |
| `image_generator.py` | `generate()` / `_generate_volcengine()` / `_resolve_volcengine_credentials()` 加 `endpoint_id_override`，优先 override 回退 config |
| `chat_image_gen.py` | 传 `endpoint_id_override=resolved.endpoint_id` |
| `listing_studio_router.py` | 传 `endpoint_id_override=resolved.endpoint_id` |
| `product_image_gen_task_use_case.py` | `create()` / `_generate_images_background()` 加 `endpoint_id_override` 并传递 |

### 测试
- chat_model_resolution_use_case: 22 passed ✅
- product_image_gen_reference_chain: 2 passed ✅
- chat_model_ref_policy: 6 passed ✅
- gateway model/catalog/credential 子集: 53 passed ✅

### 注意
- 系统模型路径（`_resolve_visible_system_image_gen`）仍依赖环境变量，因为 `list_visible_models` 返回的 dict 不含凭据 extra
- 个人/团队模型路径已完全打通：DB 凭据 extra.image_endpoint_id → RegisteredModelResolution.extra → ResolvedImageGenModel.endpoint_id → ImageGenerator.endpoint_id_override → 火山 API
- 修改需部署到远程服务器 https://gateway.giimallai.com/ai-agent/ 才生效

---

## 创建 ai-gateway-ops 技能（用户级）

- 位置：`C:\Users\Administrator\.workbuddy\skills\ai-gateway-ops\`（用户级，跨项目可用）
- 用途：通过调用 HTTP API 管理 AI Gateway 全流程（团队→凭据→模型→路由→配额）
- 内容：`SKILL.md` + 6 个 references（api_conventions / team / credential / model / route / quota operations）+ `scripts/gateway_client.py`（标准库 urllib 封装的 CLI，无第三方依赖）
- 覆盖操作：创建团队、创建团队/系统/个人凭据、探测凭据上游模型、批量导入模型到团队、查看/修改模型能力（tags）、测试模型连通性、删除模型、创建跨团队个人工作区路由、配置/查看上游限额（layer=upstream quota-rules）
- 打包产物：`E:\project\ai-agent\ai-gateway-ops.zip`
- 关键经验：skill-creator 的 `quick_validate.py` 用 `re.search(r'description:\s*(.+)', frontmatter)`（无 DOTALL）提取 description，只取 `description:` 同行内容；YAML `>-` 折叠块会被误判为含尖括号 `>`。**创建技能时 description 必须用单行，不能用 `>-` 多行折叠**。

### 后续调整：默认环境改为线上 + 环境变量说明
- 用户要求默认使用线上环境 `https://gateway.giimallai.com/ai-agent/api/v1`
- 已更新：`SKILL.md`（新增"环境变量配置"小节，含 Linux/Windows PowerShell/CMD 配置与持久化方法）、`gateway_client.py` 的 `DEFAULT_BASE_URL`、`references/api_conventions.md`
- 关键事实：技能位于 `~/.workbuddy/skills/ai-gateway-ops/`（用户级），**不依赖项目目录**，任意工作目录下都可调用
- 只需配置 `GATEWAY_TOKEN`（JWT，必需）；`GATEWAY_BASE_URL` 已内置线上默认值，仅本地开发时需覆盖

### 后续：后端支持 API Key 认证管理面 + 技能改用 API Key
- 用户要求：管理面 `/api/v1/gateway/*` 支持用平台 API Key（sk_ 开头）认证，技能改用 API Key 而非 JWT（JWT 易过期，不适合自动化）
- 后端改动：`backend/domains/identity/application/principal_service.py`
  - 新增 `_principal_from_api_key`：识别 `sk_` 前缀 → `ApiKeyUseCase.verify_api_key` 校验 → scope 校验（读需 gateway:read，写需 gateway:admin）→ 加载关联 user → 构造 Principal
  - 修改 `get_principal`：开头识别 `sk_` 前缀走 API Key 路径，否则走原 JWT/SSO 逻辑
  - 复用 user 的 RBAC 角色与团队成员关系，权限校验与 JWT 一致
  - API Key 上下文存入 `request.state.api_key_id` / `api_key_scopes` 供审计
- 技能改动：`gateway_client.py` 环境变量改用 `GATEWAY_API_KEY`（优先）+ `GATEWAY_TOKEN`（兼容回退）；SKILL.md 与 api_conventions.md 更新认证说明
- **`gateway:admin`/`gateway:read` scope 之前是"预留"未接入，本次正式启用**
- **需部署到线上 https://gateway.giimallai.com/ai-agent/ 才生效**（K8s，见 deploy/k8s/）
- 前端创建 API Key 路径：设置页 → API Key Tab → 创建 → scope 勾 gateway:admin + gateway:read（或 gateway_full 分组），plain_key 仅显示一次

### 调试：API Key 认证 500 错误
- 用户已部署后端并设置 `GATEWAY_API_KEY` 环境变量（sk_d9786afff7a60597_...），但调用 `/api/v1/gateway/teams` 等端点返回 500
- 排查：无效 sk_ key 返回 401（说明新代码已部署），但有效 API Key 返回 500（说明 `_principal_from_api_key` 内部有未捕获异常）
- 修复：在 `_principal_from_api_key` 中：
  1. 对 `verify_api_key` 步骤加 try-except，捕获任意异常 → 记录完整 traceback → 转 401 `AuthenticationError`
  2. scope 拒绝时记录 warning 日志（含 key_id、已有 scopes、method、path）
  3. 修正 `db.get(User, entity.user_id)` —— `entity.user_id` 已是 `uuid.UUID`，无需再 `_uuid.UUID()` 转换
  4. 成功认证时记录 debug 日志
- **需重新部署后才能看到效果**；部署后若仍 500，查后端日志中的 "API key verification failed unexpectedly" 或 "rejected: missing gateway scopes"

### 验证：本地重启后 API Key 认证成功
- 用户重启本地 8000 端口服务后，用 `GATEWAY_API_KEY`（sk_d9786afff7a60597_...，属用户邓烈涛 877ae63a）调用 `GET /api/v1/gateway/teams` 返回 200 + 完整团队列表
- 返回 70+ 个团队（7 个 shared + 60+ 其他用户 personal），因为该用户是平台 admin，能看全平台团队
- 用户直接相关的团队：火山-郑州夜康(owner)、研发API调用团队(admin)、武汉运营(admin)、研发IDE模型调用(admin)、我的测试团队(admin)、new(admin)、模型共享团队(member)、邓烈涛(personal/owner)
- **线上 https://gateway.giimallai.com 仍返回 500，需重新部署最新代码**
- 技能脚本 `gateway_client.py` 的 `--token` 参数不是全局参数，需放在子命令前或用环境变量 GATEWAY_API_KEY
