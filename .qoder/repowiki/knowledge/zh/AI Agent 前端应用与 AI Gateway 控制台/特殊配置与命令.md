- **开发启动**：`npm run dev` 通过 `scripts/dev-server.mjs` 启动，代理配置在 `vite.config.ts` 中处理 `/ai-agent/api` 前缀并剥离 Cookie 以避免 431 错误。
- **代码质量**：集成 Husky + lint-staged 实现提交前自动修复 (`eslint --fix`, `prettier --write`)；支持 SonarQube 扫描 (`npm run sonar`)。