- **容器化与编排**：Docker Compose V2, Kubernetes (阿里云 ACK), Helm-like patching via `kubectl patch`.
- **网关技术**：HiGress Ingress Controller, WasmPlugin (giikin-auth-bridge) for SSO.
- **同步工具**：rsync, ssh, base64-encoded remote bash execution.