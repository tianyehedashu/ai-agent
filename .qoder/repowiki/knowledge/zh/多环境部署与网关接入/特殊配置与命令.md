- **远程初始化**：`bash deploy/remote-deploy.sh --setup` 自动在目标主机安装 Docker 及 Compose 插件。
- **Wasm 插件发布**：需在 VPC 内执行 `push-wasm-docker.sh` 推送镜像，随后运行 `apply-wasmplugin.sh` 注入 Redis 密码并应用配置。
- **SSO 修复流程**：通过 `run-nacos-fix.sh` 清空 Nacos 回调地址，确保 IAM 绑定流程正确指向网关路径。