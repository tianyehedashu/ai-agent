- **启动**：`npm run dev` (端口 3000)，通过 `scripts/dev-server.mjs` 启动。
- **代码质量**：`npm run check` 执行类型检查、ESLint 和 Prettier 校验；`npm run sonar` 集成 SonarQube 扫描。
- **环境变量**：需配置 `.env` 中的 `VITE_API_URL` 指向后端服务（默认 localhost:8000）。