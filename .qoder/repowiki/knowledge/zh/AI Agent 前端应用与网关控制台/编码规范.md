- API 请求统一通过 `src/api/client.ts` 的 `apiClient` 实例发起，自动携带 Bearer Token 或 Cookie 凭证。
- 服务端数据获取优先使用 TanStack Query 的 `useQuery`/`useMutation`，并定义统一的 `query-keys` 模块以便缓存失效。
- 业务逻辑封装在 `src/features` 目录下，每个特征模块包含独立的 hooks、components 和 utils。
- UI 组件基于 Radix UI 原语构建，并通过 `class-variance-authority` 管理变体样式。