- 表现层（Presentation）通过 FastAPI 依赖注入获取应用服务（UseCase），严禁直接在路由中编写业务逻辑。
- 业务域（Domains）严格遵循 Application（用例）、Domain（实体/值对象）、Infrastructure（仓储/适配器）的分层结构。
- 配置管理采用优先级机制：环境变量 > .env 文件 > config/app.toml，敏感信息统一通过 `SecretStr` 处理。
- 异常处理遵循 RFC 7807 Problem Details 规范，通过全局异常处理器将领域错误映射为标准的 JSON 响应。