- **依赖管理**：使用 `uv` 进行极速依赖同步 (`make install-all`)。
- **开发启动**：Windows 环境下需通过 `scripts/run_server.py` 启动以强制使用 `SelectorEventLoop`，避免 psycopg/langgraph 异步连接错误 (`make dev`)。
- **数据库迁移**：开发环境使用 `make db-upgrade`，生产环境建议执行 `alembic/sql/` 下的纯 SQL 脚本。