- **开发启动**：`npm run dev`（通过 `scripts/dev-server.mjs` 启动，自动处理 API 代理与 Cookie 隔离）。
- **质量检查**：`npm run check`（串行执行类型检查、ESLint 和 Prettier 格式校验）。
- **测试与扫描**：`npm run test:coverage` 结合 `sonar-scanner` 进行代码质量扫描。