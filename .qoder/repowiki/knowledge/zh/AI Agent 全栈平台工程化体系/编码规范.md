- 所有子模块遵循统一的 React 组件组合模式与状态提升规范，避免布尔值 Props 并优先使用 Children 模式。
- 后端 Python 代码严格执行 Ruff/Pylint 静态检查，前端 TypeScript 代码遵循 ESLint 类型安全规范，并通过 CI 自动拦截违规提交。
- 数据库 Schema 变更必须通过 Alembic 迁移脚本管理，并遵循根级 `database-schema` 技能中定义的基模型与索引设计规范。
- 多环境配置采用 TOML 格式集中管理，禁止在代码中硬编码环境特定参数，确保配置与逻辑解耦。