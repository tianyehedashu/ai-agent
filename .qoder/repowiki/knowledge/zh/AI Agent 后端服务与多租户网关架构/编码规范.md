- 业务逻辑封装在 `application` 层的 UseCase 类中，仅接受抽象接口或数据库会话，不直接依赖具体仓储实现。
- 所有涉及数据访问的仓储类必须继承 `TenantScopedRepositoryBase`，以确保自动应用多租户权限过滤。
- API 路由层（Presentation）仅负责参数校验与调用 UseCase，禁止包含任何业务逻辑或数据库操作。
- 使用 `Problem Details` (RFC 7807) 格式统一处理全局异常，通过 `libs.exceptions` 定义领域错误并映射为对应的 HTTP 状态码。